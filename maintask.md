## 端侧大模型编译中的算子融合与内存复用优化研究

##### -任务：

针对端侧大模型推理时 “算子调用频繁、内存反复申请释放” 的问题，通过编译阶段的算子融合与内存复用优化，提升模型运行效率。
-研究内容：

#### ①分析端侧大模型（一般是剪枝压缩后的大模型）的计算图结构，识别可融合的算子组合（如 Conv+BN+ReLU）及内存访问密集型算子。

#### ②基于编译框架，设计算子融合规则与内存复用策略，减少算子间数据传输开销和内存碎片。

#### ③以具体任务（如端侧图像处理）为测试场景，对比优化前后的推理速度、内存峰值占用，验证优化效果。

